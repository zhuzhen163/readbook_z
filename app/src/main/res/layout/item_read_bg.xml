<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             xmlns:tool="http://schemas.android.com/tools"
             android:layout_width="wrap_content"
             android:layout_height="wrap_content"

             android:orientation="vertical">

    <ImageView
        android:id="@+id/read_bg_view"
        android:layout_width="@dimen/dimen28dp"
        android:layout_height="@dimen/dimen28dp"
        android:background="@drawable/icon_yuan1"/>

    <ImageView
        android:id="@+id/read_bg_iv_checked"
        android:layout_width="@dimen/dimen29dp"
        android:layout_height="@dimen/dimen29dp"
        android:scaleType="center"
        android:src="@drawable/background_corners_read_history"
        android:visibility="gone"
        tool:visibility="visible"
        />
</FrameLayout>